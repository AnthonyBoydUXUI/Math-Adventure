# INTEGRATION

**Who may edit:** Cursor only.  
Status: wait for `RIVE_READY`

Start only after Codex’s deliverable PR checklist is complete.  
If the `.riv` will hurt mobile performance or break the contract, **do not rewrite the file**. Record the issue here, set Rive → `blocked`, and send the PR back to Codex.

---

PASS / NEEDS CHANGES:

Contract validation:

React integration (`LiveWorld` / `LiveDay`):

Surfaces still working (Home / Map / Train / Circuit / WorldScene):

`RivePlay` untouched:

Fallback JPEG path:

Vercel Preview:
