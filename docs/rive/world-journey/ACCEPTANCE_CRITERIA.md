# ACCEPTANCE CRITERIA

**Who may edit:** Astra only.  
Status: `NEEDS_DESIGN`

Write checks Codex and Cursor can pass/fail. One line per check.

---

Must be true on Home (square):

Must be true on Train (compact `h-52`):

Must be true on Map circuit (`h-44`):

Must be true after a correct lock-in:

Must be true after a miss:

Must be true in recap:

Must be true when `prefers-reduced-motion: reduce`:

Must never happen:
