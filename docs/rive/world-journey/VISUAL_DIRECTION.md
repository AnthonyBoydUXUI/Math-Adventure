# VISUAL DIRECTION

**Who may edit:** Astra only.  
Status: `NEEDS_DESIGN`

Locked canon (do not contradict without a `DEC-*`): Harbor RS, `paint-volt` `#c4a24a`, void `#05070b`, pack navy `#0e1a3a`, bone `#f3efe6`, gold `#e4c24a`. Collector, not cartoon. No HUD or stamps inside the `.riv`.

---

Courtyard:

Harbor RS (top-down, vinyl, heading):

Light / atmosphere:

Compact crop (what must stay on screen):

What we will not draw:
