# INTERACTION DESIGN

**Who may edit:** Astra only.  
Status: `NEEDS_DESIGN`

Names and triggers are locked in [`../WORLD_JOURNEY.md`](../WORLD_JOURNEY.md). Describe feel. Do not rename inputs. If Cursor’s contract blocks a feel you need, file an inter-agent question — do not edit the contract.

---

Touch (World Journey itself is not a tap-to-rev surface):

Spatial behavior (car pinned vs courtyard moving):

Correct-answer behavior (`fireCorrect` / Boost):

Miss behavior (`fireMiss` / Brake):

Boss behavior (`phase = boss`):

Idle / warmup / recap behavior:

Reduced-motion behavior (must still match Cursor’s park rules unless a `DEC-*` changes them):

Sound (app-owned — say what you want React to play, not what to embed):
