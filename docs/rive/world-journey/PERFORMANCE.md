# PERFORMANCE

**Who may edit:** Cursor only.  
Status: wait for `INTEGRATION_IN_PROGRESS`

If the asset misses these, send the PR back. Do not “fix” the `.riv` in React.

---

Target: 60fps on a mid phone inside Train compact (`h-52`) and Home square.

| Check | Result | Notes |
| --- | --- | --- |
| Canvas resize / DPR | | |
| First bind (no flash of default Harbor at wrong progress) | | |
| No extra audio graph from the `.riv` | | |
| Memory / file size | | |
| Main-thread jank during Boost | | |

Send-back to Codex (if any):
