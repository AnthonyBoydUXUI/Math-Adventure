# Coach character

Status: closed until Harbor `QA_PASS` · Token: none

**Sequence:** Cursor constraints → Astra design → Codex `.riv` → Cursor integrate.

Do not open Design or Rive lanes yet.

| Lane | Owner | Status |
| --- | --- | --- |
| Engineering constraints | Cursor | `empty` |
| Game design / interaction / acceptance / visual | Astra | `empty` |
| Rive implementation / validation / asset status | Codex | `empty` |
| Integration / QA / performance / deployment | Cursor | `empty` |

Exists today: Signal sheets + `RivePlay` tap-to-rev on Help / Home. Player is Signal (`CAST_APPROVED`). Coach is not a second shipped Rive character.
