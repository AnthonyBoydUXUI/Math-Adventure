# `/public/rive`

| File | Status | Owner | Used by |
| --- | --- | --- | --- |
| `vehicles.riv` | `shipped` placeholder | leave alone | `RivePlay` (`bumpy` / `bump`) |
| `world-journey.riv` | **not in repo yet** | Codex, on PR `cursor/rive-world-journey-64b2` | future `LiveWorld` |

Codex commits `world-journey.riv` on that PR with [`RIVE_PR_CHECKLIST.md`](../../docs/game-production/RIVE_PR_CHECKLIST.md).  
Do not overwrite `vehicles.riv`.
